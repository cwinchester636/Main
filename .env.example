# Point the frontend at your deployed SwapDeck Worker. Defaults to
# http://localhost:8787 (a locally running `wrangler dev`) if unset.
VITE_API_BASE_URL=https://swapdeck-api.your-subdomain.workers.dev
